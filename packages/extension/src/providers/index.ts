/**
 * Providers barrel — re-exports all VS Code API providers.
 *
 * @internal Exported for module structure only; no providers
 * registered yet. Will be populated when the chat model
 * provider is implemented.
 */
export {};
