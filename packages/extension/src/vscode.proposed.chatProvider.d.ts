/**
 * Type declarations for the VS Code proposed `chatProvider` API.
 *
 * The `LanguageModelChatProvider` and `registerLanguageModelChatProvider`
 * are already defined in `@types/vscode`. This file only extends them
 * with proposed-API members that haven't been finalized yet.
 *
 * @see https://github.com/microsoft/vscode/blob/main/src/vscode-dts/vscode.proposed.chatProvider.d.ts
 */
export {};
